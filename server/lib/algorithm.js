/**
 * @description 计算1m，10m，30m，60m，4h，1d，1w，1M，1y，10y的位置变化
 * @param {Object} data
 * @returns {Object} err
 * @returns {Object} results
 */
const posChange = (data, callback) => {
  let [err, results] = [null, null]
  callback(err, results)
}
